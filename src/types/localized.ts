export type Locale = 'sv' | 'en' | 'pl';

export interface LocalizedString {
  sv: string;
  en: string;
  pl: string;
}

export interface LocalizedKeywords {
  sv: string[];
  en: string[];
  pl: string[];
}
